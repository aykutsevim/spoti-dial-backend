#ifndef LV_DISPLAY_H
#define LV_DISPLAY_H

#include <lvgl.h>

/**
 * Initialize LVGL with M5Dial display
 * Must be called before any LVGL objects are created
 */
bool lv_display_init();

#endif // LV_DISPLAY_H
